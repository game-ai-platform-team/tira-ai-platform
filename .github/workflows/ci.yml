name: CI

on:
  push:
  pull_request:
    branches: [dev]
  workflow_dispatch:

jobs:
  ci_frontend:
    uses: ./.github/workflows/ci_front.yml
  ci_backend:
    uses: ./.github/workflows/ci_back.yml
